<%= form_with(model:[book, book_comment], local: false) do |f| %>
  <%= f.text_area :comment, :size => "70x5",  placeholder:"コメント入力" %><br>
  <%= f.submit "送信", class: "btn btn-lg btn-base-1 mt-20 pull-right" %>
<% end %>
